this is the correction of exercice 34 : flatten All Items

NOTE:
always explicitly type empty arrays in TypeScript because the compiler cannot know the type when we have no elements.
const arr: number[] = [] => this is correct
const arr = [] => this is not correct
